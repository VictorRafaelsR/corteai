{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "startCommand": "/opt/venv/bin/gunicorn app:app --workers 2 --timeout 300",
    "healthcheckPath": "/",
    "restartPolicyType": "ON_FAILURE"
  }
}
